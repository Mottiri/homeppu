# 機能修正・追加バックログ (2025-12-18)

このドキュメントは、ほめっぷアプリの細かい修正と追加機能の提案をまとめたものです。

---

## 🔔 通知機能（2026-01-12 追加）

### 17. プッシュ通知タップ遷移の総点検・修正
- **優先度**: 中
- **状態**: ✅ 実装済み（確認完了 2026-01-30）
- **問題**:
  - 目標の事前通知など、タップしても詳細画面ではなく通知一覧画面に遷移してしまう
  - `main.dart` の `_handleNotificationTap` で特定の `type` を処理していないため、`default` ルートに落ちている
- **内容**:
  - 通知タイプを棚卸しし、未対応の `circle_settings_changed` / `circle_ghost_warning` / `circle_ghost_deleted` / `review_needed` を追加
  - `NotificationModel` とカテゴリ分類を更新（circle/support への分類）
  - プッシュ通知タップ（`main.dart`）と通知一覧タップの遷移を一致させた
    - `review_needed` → `/admin-review`
    - `circle_settings_changed` / `circle_ghost_warning` → サークル詳細（ID欠落時は `/circles`）
    - `circle_ghost_deleted` → `/circles`
  - ID欠落時のフォールバックを追加（`/circles` or `/notifications`）
- **期待値**: 通知一覧/プッシュ通知のどちらからでも関連画面へ遷移できる（タブ増減なし）

---

## 📋 タスク機能

### 1. カテゴリフィルタリング ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（確認完了 2025-12-18）
- **内容**: 
  - タスク一覧にカテゴリフィルターを追加
  - カテゴリをタップすると、そのカテゴリに紐づくタスクのみ表示
  - 例: ゲームカテゴリ選択 → ゲームに紐づくタスク2件のみ表示
- **実装箇所**: `tasks_screen.dart`
  - TabBarでカテゴリタブを動的生成
  - TabBarViewでカテゴリごとにタスクリストを表示
  - 「+」タブで新規カテゴリ追加可能

### 2. 月間カレンダーの完了タスク優先非表示 ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-12-18）
- **内容**:
  - 複数タスクがある日で「+9」などで省略される場合
  - 完了済みタスクを優先的に省略（非表示）にする
  - 未完了タスクを優先的に表示
- **実装箇所**: `monthly_calendar_screen.dart`
  - タスクリストを未完了優先でソート
  - 同じ完了状態なら優先度で降順ソート

### 3. タスク表示設定機能
- **優先度**: 中
- **状態**: ⏸️ 保留
- **内容**:
  - タスク画面のヘッダーに設定アイコン（⚙️）を配置
  - タップでボトムシート表示
  - 表示/非表示の切替項目:
    - メモを表示 [ON/OFF]
    - 繰り返しアイコン [ON/OFF]
    - 添付画像サムネイル [ON/OFF]
    - リマインダーアイコン [ON/OFF]
    - サブタスク表示 [ON/OFF]
  - 設定は端末に保存（SharedPreferences）

### 3. カスタム画像設定（アイコン・ヘッダー） ✅
- **優先度**: 中
- **状態**: ✅ 完全実装済み
- **内容**:
  - サークル作成時・編集時にオーナーがアイコン・ヘッダー画像を設定可能
  - `image_cropper`でアスペクト比固定（アイコン1:1、ヘッダー16:9）
  - 削除ボタン（×）でデフォルトに戻る
  - Firebase Storageに`circles/{circleId}/icon/`または`cover/`で保存
  - ✅ AIモデレーション（保存時に不適切画像をブロック）
  - ✅ Storage画像削除（サークル削除時）← `cleanupDeletedCircle`で実装済み

### 4. 「プログラミング」カテゴリの削除 ✅
- **優先度**: 低
- **状態**: ✅ 実装済み（2025-12-19）
- **内容**:
  - サークルカテゴリから「プログラミング」を削除
  - ターゲット層に合わせたカテゴリ整理

### 5. タスク編集画面でカテゴリ変更可能に ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-12-19）
- **内容**:
  - 現在タスク作成後はカテゴリを変更できない
  - タスク編集画面でカテゴリも変更可能にする

### 6. ストリーク自動投稿機能 ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（2025-12-28）
- **内容**:
  - 繰り返しタスクの連続達成（ストリーク）を記録
  - 節目達成時（3日, 7日, 14日, 21日, 30日ごと, 100日ごと, 365日ごと）に自動投稿
  - 目標達成時にも自動投稿
  - 紙吹雪アニメーション + 称賛Snackbar
  - タスクカードに🔥アイコン + 数字でストリーク表示
  - 完了取り消し時、自動投稿を自動削除
- **設計書**: `task_feature_design.md` セクション13, `goal_feature_design.md` セクション6

---

## ❤️ リアクション機能

### 5. リアクションアイコンのカテゴリ分け ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（2025-12-18）
- **内容**:
  - リアクションボタン押下時、アイコンをカテゴリ別に整理
  - 画面いっぱいに表示されず、コンパクトなタブ形式で表示
- **実装箇所**: 
  - `app_constants.dart` - `ReactionCategory` enum（5カテゴリ）
  - `reaction_selection_sheet.dart` - タブ形式でカテゴリ切り替え
  - `recent_reactions_service.dart` - 直近使用リアクション管理
- **カテゴリ**:
  - ⏱️ **よく使う**（直近使用した最大7件、履歴がある場合のみ表示）
  - ❤️ おすすめ（❤️✨💪🤝🎈☺️🍌）
  - ⭐ 記号（⭐💗💙🔥👍👏など）
  - 😊 表情（😊😆😂😍🥳など）
  - 🌸 自然・生き物（🐱🐶🌸🌈など）
  - 🎁 食べ物・アイテム（🎁🏆☕🍰🍣など）

### 6. リアクション時のアニメーション強化 ✅
- **優先度**: 中
- **状態**: ✅ 完全実装済み（2025-12-21）
- **内容**:
  - リアクション押下時のアニメーション演出
  - **仕様**: リアクション直後は大きいサイズのアイコン → だんだん小さくなる → 背景に溶け込んで最終サイズになる（300ms）
  - ~~AI・ユーザー問わず全員に見えるアニメーション~~ → リアクションを付けた人のみ表示
- **シート連続操作**: シートを閉じずに複数回リアクション可能。5回でシート自動クローズ+トースト表示。
- **実装箇所**: 
  - `reaction_background.dart` - StatefulWidget、新規リアクション検知時にスケールアニメーション
  - `reaction_selection_sheet.dart` - 連続操作対応、5回カウント
  - `post_card.dart` - ローカルリアクション状態管理、5回制限チェック

> [!NOTE]
> **決定**: 全員に見えるアニメーションはちらつき問題があるため、リアクション追加者のみに変更（2025-12-19）
> **更新**: 背景スタンプアニメーション復元、シート連続操作対応（2025-12-21）

### 7. リアクション回数制限 ✅
- **優先度**: 高
- **状態**: ✅ 完全実装済み（2025-12-21）
- **内容**:
  - 1人あたり1投稿に対して最大5回まで
  - スパム防止
- **実装箇所**: 
  - `functions/src/index.ts` - `addUserReaction`関数（バックエンド制限・セキュリティ）
  - `post_card.dart` - シート表示前にFirestoreから既存リアクション数を確認、5回でトースト表示してシートを開かない
  - `reaction_selection_sheet.dart` - シート内で5回リアクション後、シート自動クローズ+トースト表示

### 8. AIリアクション数の調整 ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-12-18）、✅ 重複防止追加（2025-12-19）
- **内容**:
  - AIは1投稿に1回のみリアクション
  - **5〜15件（平均10件）**に調整
  - 理由: 30件は通知が多すぎる、MIXモードへの誘導のため
- **実装箇所**: `functions/src/index.ts` - `onPostCreated`関数
- **重複防止**: `generateAIReactionV1`で既存リアクションをチェックし、同じAIが2回以上リアクションしないよう修正

> [!NOTE]
> **変更履歴**: 15〜30件 → 5〜15件（2025-12-18）
> **バグ修正**: 同じAIが複数回リアクションする問題を修正（2025-12-19）

### 8-1. リアクション背景スタンプの表示範囲修正
- **優先度**: 中
- **状態**: ✅ 実装済み（確認完了 2026-01-30）
- **問題**:
  - リアクション時に投稿カード背景にスタンプがたまっていくが、たまにカード外（見えない部分）にスタンプが押されている
  - ユーザーが押したスタンプがカード内に収まらないケースがある
- **修正内容**:
  - スタンプの配置位置をカード内に収めるよう座標計算を修正
  - カード境界からのマージンを設けてオーバーフローを防止
  - レア度はグローで表現（枠は視認性が落ちるため不採用）
- **将来拡張（レアスタンプ）**:
  - サブスク課金者向けにレアスタンプ機能を追加予定
  - レアスタンプの場合、背景スタンプのサイズを大きく表示
  - 限定アニメーション（キラキラエフェクト等）を追加
- **実装箇所**: `reaction_background.dart`
- **追加日**: 2026-01-14
  - add opacity cap for crowded stamps (base opacity <= 0.12)
  - limit max displayed stamps (default 40)


### 8-2. レアスタンプのサイズ・個別アニメーション調整
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-02）
- **背景**:
  - レアスタンプのサイズ倍率や専用アニメーションは未実装のため、8-1完了だけでは認識齟齬のリスクがある
- **対応内容**:
  - Rare / Epic のスタンプサイズ倍率を設計・実装
  - Rare / Epic の個別アニメーション（例: パルス/キラキラ等）を設計・実装
  - 実機で見え方を確認し、倍率や演出を調整
- **実装箇所**: `reaction_background.dart`, `post_card.dart` ほか（必要に応じて）
- **追加日**: 2026-01-30


### 9. 長押しリアクションUI ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（2025-12-25）
- **内容**:
  - 投稿カード長押しでLINE風オーバーレイ表示
  - スタガードアニメーション（スタンプが1つずつぽぽんと出現）
  - カスタムアセット対応（7種類: heart, praise, shine, clap, star, hundred, rainbow）
  - 背景タップで閉じる、5回で自動閉じ
- **実装箇所**: `post_card.dart`

### 10. カスタムスタンプ機能（将来）
- **優先度**: 中
- **状態**: 未実装
- **対象**: サブスク課金者限定
- **内容**:
  - ユーザーが独自スタンプ画像を登録
  - 作成者本人のみ使用可能
  - ファイルサイズ制限あり
- **審査フロー**:
  1. システム審査（AI画像審査で違反拒否）
  2. 管理者目視審査（許可/拒否）
  3. 通知（許可: 使用可能、拒否: 理由付き）
- **設計書**: `docs/design/reaction_system_design.md` セクション7

### 11. 新着投稿ドット表示（廃止）
- **優先度**: -
- **状態**: ❌ 廃止（2025-12-25）
- **理由**: AIモードフィルタリングとFirestoreスナップショット監視の不整合により誤検知が頻発
- **代替案**: ユーザーがプルダウンで手動更新

### 12. 拡張可能スタンプバー ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（2025-12-25）
- **内容**:
  - 5つのスタンプ + ＋アイコン（右端）
  - ＋押下で残りスタンプを縦長リストで表示
  - 横スクロール対応（オーバーフロー防止）
  - 拡張リストにもポップアニメーション
- **実装箇所**: `post_card.dart` - `_ReactionOverlayDialog`

### 13. スタンプ昇格機能 ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-12-25）
- **内容**:
  - 使用したスタンプが次回表示時にバーの先頭に移動
  - 最も使用していないスタンプが＋アイコン内に格納
  - `RecentReactionsService`で使用順を永続化
- **実装箇所**: `post_card.dart`, `recent_reactions_service.dart`

---

## 👤 プロフィール画面

### 9. 賞賛カウントの修正（バグ）✅
- **優先度**: 高
- **状態**: ✅ 完了（2025-12-18）
- **内容**:
  - 「賞賛」の項目がカウントされていない問題を修正
- **実装内容**:
  - `onReactionCreated` トリガーを Cloud Functions に追加
  - リアクション追加時に投稿者の `totalPraises` を自動インクリメント
  - 自己リアクションはカウントしない

### 10. 投稿カードのスマート化
- **優先度**: 中
- **状態**: ✅ 実装完了
- **内容**:
  - プロフィール画面の過去投稿にメディアアイコン（📷/🎥）を追加
  - リアクション背景スタンプを表示（実際に付いたリアクションを可視化）
  - タップで投稿詳細画面へ遷移
- **実装ファイル**: 
  - `profile_screen.dart`: _ProfilePostCardにメディアアイコンとReactionBackground追加
- **完了日**: 2025-12-27

### 11. お気に入り投稿機能
- **優先度**: 中
- **状態**: ✅ 実装完了
- **内容**:
  - プロフィール画面の過去投稿をお気に入り登録
  - 3タブ構成（アイコン表示: 🏠TL / 👥サークル / ⭐お気に入り）
  - 三点メニューから「お気に入りに追加/解除」
  - お気に入りは投稿日時順で表示
- **実装ファイル**: 
  - `post_model.dart`: isFavoriteフィールド追加
  - `profile_screen.dart`: 3タブ・アイコン表示・お気に入りリスト・切替機能
- **完了日**: 2025-12-27

> [!NOTE]
> **決定**: 名称は「お気に入り」で進行（2025-12-18）

### 12. 過去投稿のサークル投稿区別 ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-12-27）
- **内容**:
  - プロフィール画面の過去投稿を3タブで区別
  - 🏠 TL：一般投稿（circleId == null）
  - 👥 サークル：サークル投稿（circleId != null）
  - ⭐ お気に入り：お気に入り登録した投稿
- **実装箇所**: `profile_screen.dart` - `_UserPostsList`クラス

### 13. プロフィール欄のメッセージボタン削除
- **優先度**: 低
- **状態**: ⬜ 未着手
- **内容**:
  - DM機能は未実装のため、プロフィール画面のメッセージボタンを削除する
  - ボタンが存在すると、ユーザーにDM機能があるように誤解される
- **追加日**: 2026-02-16

---

## ✏️ 投稿画面

### 12. 添付アイコンの変更
- **優先度**: 低
- **状態**: ✅ 実装済み（2026-01-29）
- **内容**:
  - クリップアイコン → 「+」アイコンに変更

### 13. 投稿詳細画面でメディアを表示 ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（2025-12-19）
- **内容**:
  - 投稿一覧のカードにはメディア（画像・動画）が表示されている
  - ~~**問題**: 投稿詳細画面ではメディアが見えない~~
  - ✅ 詳細画面でもメディアを閲覧可能にする
  - プロフィール画面の過去の投稿カードに、添付メディアがある場合アイコンとして表示して、一目でわかるようにする
- **実装箇所**: `post_detail_screen.dart`
  - `_buildMediaSection`メソッドでメディア表示
  - 画像タップでフルスクリーン表示（InteractiveViewer）

### 14. 動画サムネイル表示
- **優先度**: 中
- **状態**: ✅ 実装完了
- **内容**:
  - 動画アップロード時に最初のフレームをサムネイルとして自動生成
  - サムネイルをFirebase Storageにアップロード
  - 投稿一覧でサムネイル画像を表示
- **技術詳細**:
  - サムネイルサイズ: 最大高さ800px、JPEG品質90%
  - Firebase Storage: `posts/{userId}/thumbnails/`パスに保存
- **実装ファイル**: 
  - `post_model.dart`: MediaItemにthumbnailUrlフィールド追加
  - `media_service.dart`: _generateAndUploadThumbnailメソッド追加
  - `post_card.dart`: サムネイル表示ロジック更新
  - `firebase/storage.rules`: thumbnailsパスへの権限追加
- **完了日**: 2025-12-27

---

## 🔔 通知画面

### 13. 通知のカテゴリ分け
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-12-21）
- **内容**:
  - タブで4カテゴリに分類:
    - **すべて**: 全通知
    - **TL**: コメント、リアクション
    - **サークル**: 参加申請、承認、拒否、削除
    - **タスク**: リマインダー、予定時刻通知
  - 各タブに未読バッジ表示（赤丸で件数表示）
  - スクロール対応（狭い画面でも横スクロール可能）

---

## 🛡️ 管理者機能

### 14. 管理者専用機能
- **優先度**: 低（運用フェーズでは必須）その他機能を作り終えたら高
- **状態**: ✅ 実装済み（2025-01-05確認）
- **内容**:
  - ✅ BAN権限（`banUser` 実装済み）
  - ✅ 通報一覧確認（実装済み 2026-02-04）
  - ✅ 要審査投稿レビュー（実装済み）
  - ✅ **問い合わせ機能（実装済み 2025-12-29）**
  - 管理者アカウントのみに表示

### 15. 問い合わせ・要望機能 ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（2025-12-29）
- **設計書**: `docs/design/admin_features.md` セクション5
- **内容**:
  - ユーザーからの問い合わせ・機能要望を受け付け
  - カテゴリ: バグ報告 / 機能要望 / アカウント関連 / その他
  - チケット形式（複数往復チャット可）
  - 管理者が返信・ステータス変更可能
  - 画像添付可能
- **通知連携** ✅:
  - 新規問い合わせ → 管理者にプッシュ通知
  - ユーザー返信 → 管理者にプッシュ通知
  - 運営返信 → ユーザーにプッシュ通知
  - ステータス変更 → ユーザーにプッシュ通知
  - 通知画面に「サポート」タブ追加

### 15-1. 問い合わせチャットUX改善 ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2026-01-10）
- **内容**:
  - **画像フルスクリーン表示**: 添付画像タップでフルスクリーン表示（ピンチズーム・Heroアニメーション対応）
  - **閲覧中通知抑制**: チャット画面を開いている間は相手からの返信通知を抑制（通知スパム防止）
    - ユーザー画面オープン中 → 管理者返信の通知をスキップ
    - 管理者画面オープン中 → ユーザー返信の通知をスキップ
- **実装箇所**:
  - `lib/shared/widgets/full_screen_image_viewer.dart` - フルスクリーン画像表示ウィジェット
  - `lib/shared/services/inquiry_service.dart` - `setUserViewing`, `setAdminViewing` メソッド追加
  - `firebase/firestore.rules` - `userViewing` フィールド更新許可
  - `functions/src/index.ts` - `sendInquiryMessage`, `sendInquiryReply` に閲覧中チェック追加

### 16. 問い合わせ自動クリーンアップ ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-01-05確認）
- **設計書**: `docs/design/admin_features.md` セクション5.6.1, `docs/design/cleanup_processing_design.md`
- **内容**:
  - 解決済みから7日経過で自動削除
  - 削除対象: 問い合わせ本体 + メッセージ + 添付画像
  - `archivedInquiries`に1年間アーカイブ保存
  - 削除前日にユーザーへプッシュ通知

### 17. Googleスプレッドシート連携 ✅
- **優先度**: 中
- **状態**: ✅ 実装済み（2025-01-05確認）
- **設計書**: `docs/design/admin_features.md` セクション5.6.2
- **内容**:
  - 解決時にGoogle Sheets APIで自動追記
  - 保存項目: 日時, UID, カテゴリ, 件名, 内容, 解決日
  - 機能要望の長期管理に使用
- **必要設定**:
  - Google Cloud Console でサービスアカウント作成
  - Sheets API 有効化
  - Secret Manager にJSONキー登録

---

## 🤖 AI/Gemini API

### 15. Gemini APIエラー対策
- **優先度**: 高
- **状態**: ✅ 実装完了
- **内容**:
  - **OpenAI/Geminiフォールバック機能**:
    - `AIProviderFactory`クラスで両プロバイダーをサポート
    - Firestore設定（`settings/ai`）でプライマリプロバイダー切り替え可能
    - プライマリ失敗時に自動でフォールバック
  - **実装ファイル**: `functions/src/ai/provider.ts`
- **完了日**: 2025-12-26

---

## 🎨 UI全般

### 21. UI改善・全体ブラッシュアップ
- **優先度**: 高
- **状態**: ✅ 完了（2026-01-29）
- **内容**:
  - 全体的なデザインの統一感向上
  - アニメーションの追加・調整（マイクロインタラクションの強化）
  - 各画面のパディング、マージン、フォントサイズの微調整
  - レスポンシブ対応の確認
  - ユーザー体験（UX）を向上させるための導線整理
  - 機能改修・追加完了後に着手予定

---

## 💰 マネタイズ

### 16. 広告表示
- **優先度**: 中
- **状態**: ✅ 実装完了（機能確認済み・テストID運用）
- **内容**:
  - AdMob（テストID）を導入
  - バナー表示場所: TL / タスク / 投稿作成（いずれもAppBar直下）
  - サブスク加入者は広告非表示
  - Epicスタンプは広告視聴で一時解放可能
    - 1回の広告視聴で25回分
    - 有効期限は初回視聴から24時間（期限切れで残数リセット）
  - Cloud Functions側でリアクション使用時の残数消費を強制
  - 投稿時インタースティシャル広告（非サブスク対象）
    - 3投稿に1回のみ表示
    - 日次上限8回
    - 投稿前案内ダイアログを表示（「今後表示しない」が未設定の間は毎回表示）
    - 「今後表示しない」はユーザー単位で保存（同一端末の別アカウントへは引き継がない）
    - 広告未ロード/表示失敗時でも投稿は継続
    - 広告表示に成功した場合は、広告終了後に投稿結果を表示
  - TLフィード内ネイティブ広告（非サブスク対象）
    - 10投稿ごとに1件挿入
    - 投稿カードに寄せたUIで表示（広告ラベル付き）
    - Androidで実装（AdMobテストID / NativeAdFactory）
  - 実機確認済み
    - 非サブスク: バナー / ネイティブ / 投稿時インタースティシャル表示
    - サブスク: バナー / ネイティブ / 投稿時インタースティシャル / 投稿前ダイアログが非表示

### 16-1. AdMob本番ID切替
- **優先度**: 高
- **状態**: ⬜ 未着手
- **内容**:
  - AdMob本番IDへ切替（Banner / Interstitial / Native）
  - Android `AndroidManifest.xml` の AdMob App ID を本番値へ更新
  - リリースビルドでテストIDが残っていないことを確認
- **実装箇所**: `lib/shared/ads/ad_config.dart`, `android/app/src/main/AndroidManifest.xml`

### 16-2. 広告本番配信の受け入れ確認
- **優先度**: 高
- **状態**: ⬜ 未着手
- **内容**:
  - 本番広告配信での表示確認（TLバナー / TLネイティブ / 投稿時インタースティシャル）
  - サブスク加入者に広告が出ないことを再確認
  - AdMob管理画面で配信・インプレッション・収益計測の反映確認
  - 異常時のフォールバック（広告未取得でも投稿継続）を最終確認

### 17. サブスクリプション課金
- **優先度**: 中
- **状態**: ✅ 実装済み（内部テスト中）
- **現在の進捗（2026-02-04）**:
  - Google Play Consoleで定期購入商品を作成・有効化済み
    - 商品ID: `homeppu_premium_monthly`
    - ベースプランID: `monthly`
    - 価格: 500円/月
  - AABアップロード済み（内部テスト配布）
  - RevenueCat連携の設定完了
    - サービスアカウント連携OK
    - Webhook URL登録済み（テストイベント200確認）
    - `REVENUECAT_WEBHOOK_SECRET` 設定済み
  - アプリ側に課金画面を実装済み
    - 設定画面とサブスク限定ダイアログから `/premium` に遷移
    - 購入処理中のローディング表示 / 成功トースト / 失敗ダイアログ
    - 購入成功後、`isSubscriber` 反映待ち中は購入ボタンをローディング表示+無効化（再押下防止）
    - 失敗時は問い合わせ画面への導線あり
  - RevenueCatログイン連携はアプリ起動時に実行
  - RevenueCatのAPIキー管理用に `config/revenuecat.json` を作成
    - 用途: `REVENUECAT_API_KEY` をファイルから読み込むため
    - 注意: ファイルは `.gitignore` 済み（リポジトリに含めない）
    - 起動コマンド（開発時）:
      - `flutter run --dart-define-from-file=config/revenuecat.json`
    - ビルドコマンド（AAB）:
      - `flutter build appbundle --release --dart-define-from-file=config/revenuecat.json`
  - Webhook側は `app_user_id` の揺れ（TRANSFER/aliases/original_app_user_id）にも対応
  - サブスク解除時はサーバー側で名前パーツ/アバターの自動フォールバックを実施
- **内容（要議論）**:
  - Paywall（プレミアム画面）のUIブラッシュアップ（余白・配色・配置の整理）

### 課金モデル（決定版）
| 無料 | サブスク（プレミアム） |
|------|----------------------|
| 全タスク機能 | 広告OFF |
| 基本名前パーツ | レア名前パーツ解放 |
| 基本アバター | 限定アバターパーツ |
| サークル参加 | サークル作成数増加 |
| - | **投稿装飾機能** |
| - | [プレミアム][課金者] バッジ |

### 投稿装飾機能（技術確認済み）
- **技術的に実装可能** ✅
- **実装方法**: `Stack` + `Positioned` widgetでカード角に装飾配置
- **装飾例**:
  - 王冠（プレミアムバッジ）
  - キラキラ（パーティクルエフェクト）
  - 豪華フレーム
  - 背景パターン

### 18. サブスク管理サービス検討
- **優先度**: 中
- **状態**: ✅ 決定（2026-02-03）
- **決定**: RevenueCatを採用
- **理由**:
  - 個人開発でコード監査が難しく、お金が関わる部分は運用/セキュリティを最優先したい
  - Webhookでサーバー側の購読状態を一元管理でき、ミス・抜け漏れを減らせる
  - 将来的にiOS対応する場合でも同一運用で拡張できる
- **RevenueCatのメリット**:
  - iOS/Android両対応を統一管理
  - ダッシュボードで顧客・収益を可視化
  - 返金処理の自動追跡
  - Webhookでサーバー側と連携可能
  - 無料枠あり（月$2,500収益まで）
- **補足**:
  - Google Play Consoleの定期購入商品設定は必須（RevenueCatは商品IDを参照するため）
  - Webhook設定は完了（テストイベント200確認済み）
- **公式**: https://www.revenuecat.com/

### 19. BANユーザーのサブスク対応
- **優先度**: 高（サブスク実装時）
- **状態**: ✅ 完了（運用方針決定）
- **方針**:
  1. **運用方針**: BAN時は運営メッセージで「定期購入の解約」を案内する運用で統一
  2. **導線**: Paywallの「定期購入を管理」ボタンへ誘導して手動解約を促す
  3. **機能制限**: 追加のサブスク特典制限は行わず、案内運用でカバー
- **技術的制約**:
  - Google Play定期購読はサーバーから強制解約**不可**
  - 返金はGoogle Play Consoleから手動処理
- **利用規約への記載事項**:
  - 「規約違反によるアカウント制限時、サブスクリプションの返金は行いません」
  - 「制限中も閲覧機能はご利用いただけます」

---

## 📛 名前パーツ

### 18. アイコン位置の調整
- **優先度**: -（対応不要）
- **状態**: 現状維持に決定
- **内容**:
  - 現在のアイコン位置（真ん中）を維持

> [!NOTE]
> **議論点**: アイコンがあることで視覚的な識別性が上がり、他SNSとの差別化になる。
> 現状維持を推奨。

---

## 🔧 リファクタリング

### 19. サークルカテゴリ定義の統一 ✅
- **優先度**: -（対応完了）
- **状態**: ✅ 完了（2025-12-19）
- **内容**:
  - `circle_service.dart`, `circles_screen.dart`, `circle_detail_screen.dart` の3箇所に重複していたカテゴリ定義を `CircleService` に一本化
  - 変更時の修正箇所を1箇所に集約し、不整合（バグ）を防止
- **重複の原因**:
  - 初期開発段階で、一覧画面と詳細画面の実装が独立して進められた際、それぞれでカテゴリ用のMapを定義してしまい、共通のServiceを参照する設計になっていなかったため。


### 20. サークル一覧のUX改善（無限スクロール・Pull to Refresh） ✅
- **優先度**: 高
- **状態**: ✅ 実装済み（2025-12-24）
- **内容**:
  - **Pull to Refresh**: 一覧を下に引っ張って更新する機能を追加
  - **無限スクロール**: スクロール最下部で自動的に次の件数を読み込む機能を追加
  - **タブ切替時の自動更新**: カテゴリタブを切り替えた際にリストをリフレッシュ
- **実装箇所**:
  - `circle_service.dart`: `getPublicCircles`にページネーション対応（`lastDocument`, `limit`）
- `circles_screen.dart`: `RefreshIndicator`, `ScrollController`による制御

---

## 👥 サークル

### 1. ピン留め状態の不整合（一覧とピン留め一覧で差異）
- **優先度**: 中
- **状態**: ✅ 完了（2026-01-29）
- **問題**:
  - ピン留め一覧で解除した投稿が、投稿一覧の三点メニューでは「ピン留め解除」表示のままになることがある
- **再現手順**:
  1. 投稿一覧から任意の投稿をピン留め
  2. もう1つ投稿をピン留めし、ピン留めが複数の状態にする
  3. ピン留め一覧を開き、三点メニューでピン留め解除
  4. 投稿一覧で該当投稿の三点メニューを開く
  5. 解除済みにもかかわらず「ピン留め解除」が表示される
- **期待値**:
  - ピン留め解除後、投稿一覧側の状態も即時で同期される

### 2. 投稿詳細画面からピン留め操作ができない（UX）
- **優先度**: 低
- **状態**: ✅ 完了（2026-01-29）
- **問題**:
  - 投稿詳細画面からピン留め/解除ができず、一覧に戻って対象投稿を探す必要がある
- **期待値**:
  - 投稿詳細画面でもピン留め/解除が可能

---


### 21. 旧通報画面のコード削除（通知遷移は新画面のみ）
- **優先度**: 高
- **状態**: ✅ 完了（2026-01-29）
- **内容**:
  - 旧通報詳細画面（`AdminReportDetailScreen`）は現在不要
  - 通報通知は `contentId` を持つ新画面へ遷移するため、旧画面/旧ルーティングは削除する
  - 過去通知で `contentId` が無い場合も旧画面に落ちないようにする

---

## 🔒 セキュリティ

### 23. Firebase App Check の有効化
- **優先度**: 中
- **状態**: ✅ 完了（Android強制ON完了 / iOS未対応 2026-02-05）
- **問題**:
  - 現在 App Check は enforcement=disabled で動作しており、すべての Callable 関数で警告ログが発生
  - `Callable request verification failed: AppCheck token was rejected.`
  - `Allowing request with invalid AppCheck token because enforcement is disabled`
- **内容**:
  - Firebase コンソールで App Check を有効化
  - Android: Play Integrity API または SafetyNet 設定
  - iOS: DeviceCheck または App Attest 設定
  - Cloud Functions で App Check enforcement を有効化
  - テスト環境用のデバッグトークン設定
  - **進捗**: Android SDK導入（クライアントのみ）
  - **進捗**: Firestore/Storageの適用をConsoleで有効化（Android、2026-02-05）
  - **進捗**: Callable FunctionsのenforceAppCheck有効化（デプロイ済み 2026-02-05）
  - **進捗**: Android実機で強制ON動作確認済み（Play配布経由 2026-02-05）
  - **未対応**: iOS App Attest/DeviceCheck 対応
  - **未対応**: iOS配布 → 強制ON
- **AppCheck適用（Callable一覧）**:
  - admin: cleanUpUserFollows, deleteAllAIUsers, cleanupOrphanedCircleAIs, setAdminRole, removeAdminRole, banUser, backfillPublicUsers, permanentBanUser, unbanUser
  - ai: initializeAIAccounts, generateAIPosts
  - circles: deleteCircle, approveJoinRequest, rejectJoinRequest, sendJoinRequest, joinCircle, leaveCircle
  - comments: createCommentWithModeration, addUserReaction, removeUserReaction
  - inquiries: createInquiry, sendInquiryMessage, sendInquiryReply, updateInquiryStatus
  - names: initializeNameParts, getNameParts, updateUserName
  - posts: createPostWithRateLimit, createPostWithModeration
  - reports: reportContent
  - tasks: createTask, getTasks
  - users: followUser, unfollowUser, getFollowStatus, getVirtueHistory, getVirtueStatus
  - virtue_shop: getVirtueShopConfig, purchaseVirtueItem
  - circle-ai: triggerCircleAIPosts
  - http: moderateImageCallable
  - scheduled: triggerEvolveCircleAIs
- **参考**: https://firebase.google.com/docs/app-check
- **追加日**: 2026-01-14

---

## 🚫 BAN機能（2026-01-14 追加）

### 24. 被BANユーザーチャット画面に戻るボタンがない
- **優先度**: 高
- **状態**: ✅ 完了（2026-01-29）
- **問題**:
  - 被BANユーザーが運営とのチャット画面を開くと、戻るボタンがない
  - ユーザーがアプリ内で戻る手段がない
- **内容**:
  - BANチャット画面（ban_appeal_chat_screen.dart）にAppBarと戻るボタンを追加
  - マイページへ戻れるようにする
  - 補足: **永久BANユーザーは戻り先が無いため、戻るボタンを非表示にする**
- **追加日**: 2026-01-14

### 25. 永久BANユーザーのログイン挙動改善
- **優先度**: 高
- **状態**: ✅ 完了（2026-01-29）
- **問題**:
  - 現在、永久BANユーザーは Firebase Auth が disabled になりログイン自体ができない
  - 「ごめんね、うまくいかなかったみたい」というエラーが表示される
- **理想的な挙動**:
  - 永久BANでもログインは許可する
  - ログイン後はマイページと運営チャット画面のみ表示
  - BAN理由の確認、異議申し立て、データ確認が可能
- **実装案**:
  1. `permanentBanUser` から `auth().updateUser(disabled: true)` を削除
  2. `banStatus: "permanent"` のみでFirestore側で判定
  3. アプリ側でルーティング制御（マイページ + BANチャットのみ）
  4. 180日後の削除処理はそのまま維持
- **追加日**: 2026-01-14

---

## 更新履歴

| 日付 | 内容 |
|------|------|
| 2025-12-18 | 初版作成 |
| 2026-01-10 | 問い合わせチャットUX改善を追加、設計書リンク追加 |
| 2026-01-13 | 通報通知の遷移先修正、旧画面削除を追加 |
| 2026-01-14 | App Check有効化を追加、BAN機能改善2件追加 |
| 2026-02-05 | App CheckのAndroid強制ON完了、iOS未対応を明記 |
| 2026-02-06 | 徳ポイントによるBAN廃止のバックログを追加 |
| 2026-02-06 | 投稿ボーナス（テスト）+100 を `createPostWithModeration` に追加 |
| 2026-02-06 | 徳ポイントの加点/減算ヘルパー集約を記載 |
| 2026-02-07 | スタンプ登録スクリプトの差分更新/削除連動運用を追記 |
| 2026-02-07 | 徳ポイント購入ダイアログUI/運用整理（完了）を追加 |
| 2026-02-07 | テスト投稿ボーナス（+100）の本番前撤去チェックを追加 |
| 2026-02-07 | アバターパーツ購入ダイアログ統一と初回読み込み安定化（完了）を追加 |
| 2026-02-07 | アバターパーツ登録スクリプトを差分更新/削除連動/Firestore整合チェック対応に更新 |
| 2026-02-07 | 名前パーツ購入ダイアログを新UIへ統一（完了）を追加 |
| 2026-02-07 | 徳ポイント仕様見直しに「徳ポイントBAN廃止」を統合（重複タスク整理） |
| 2026-02-07 | 投稿時インタースティシャル広告（3回に1回/日次8回/初回案内）を追加 |
| 2026-02-08 | 投稿前案内ダイアログを「未スキップ時は毎回表示」に変更し、スキップ設定をユーザー単位化 |
| 2026-02-08 | TLフィード内ネイティブ広告（10投稿ごと1件・Android）を追加 |
| 2026-02-08 | サブスク購入後の反映待ち中、購入ボタンの再押下を抑止（ローディング表示） |
| 2026-02-08 | 広告表示タスクを完了化し、本番ID切替/本番配信確認を独立タスク化 |
| 2026-02-08 | 徳ポイント仕様見直しに「獲得トースト非表示」方針を追記 |
| 2026-02-08 | 徳ポイントの付与経路・具体スコア・目標証跡再審査フローを確定追記 |
| 2026-02-08 | タスクストリーク付与の判定基準を繰り返しグループ基準へ修正し、タスク自動投稿で投稿ボーナスを付与しないよう変更 |
| 2026-02-09 | #33 をスタンプシート拡張仕様へ更新（投稿主お礼いいね/回数付与/タスク導線置換） |
| 2026-02-11 | スタンプシートにアーカイブ一覧画面（`/stamps/archives`）を追加 |
| 2026-02-12 | タスク/目標クローズ作業を更新（不要導線・通知・関数・ルール・資料残骸の最終整理、監査完了） |
| 2026-02-14 | サークル作成時の説明文修正を追加 |
| 2026-02-14 | 非サブスク向けサークル初回閲覧（1回限定・閲覧専用）をチュートリアルから分離 |
| 2026-02-17 | 新規アカウント登録時のアバター未反映バグを追加 |
| 2026-02-17 | サークル画面遷移時のローディング表示を追加 |



---

##  サークル機能（2026-01-29 追加）

### 26. サークルフィルターに招待制/公開選択を追加
- **優先度**: 中
- **状態**: ✅ 完了（2026-01-29）
- **内容**:
  - サークル一覧画面のフィルターに「招待制」「公開」を選択できる項目を追加
  - ユーザーが参加可能なサークルを絞り込みやすくする
- **実装箇所**: `circles_screen.dart`, `circle_service.dart`
- **追加日**: 2026-01-29
- **補足**:
  - フィルターはボトムシート表示に変更（複数選択後に「適用」で反映）
  - 公開 / 招待制 は相互排他（どちらかを選ぶともう一方は自動で外れる）
  - 小さい端末でもオーバーフローしないようスクロール対応

### 43. サークル作成時の説明文修正
- **優先度**: 中
- **状態**: ⬜ 未着手
- **内容**:
  - サークル作成時の説明文（ガイドテキスト・プレースホルダー等）を見直し・修正
- **追加日**: 2026-02-14

### 47. サークル画面遷移時のローディング表示
- **優先度**: 中
- **状態**: ⬜ 未着手
- **問題**:
  - サークル（ボトムナビ等）を押下した後、サークル画面が表示されるまでしばらく待機が発生する
  - 待機中に何も表示されないため、ユーザーがタップが反応していないと感じる
- **期待値**:
  - ローディングインジケーターやスケルトンUI等で、画面読み込み中であることが視覚的にわかるようにする
- **追加日**: 2026-02-17

---

##  UI/UX（2026-01-29 追加）

### 27. タスク画面のステータスバー表示問題
- **優先度**: 中
- **状態**: ✅ 完了（2026-01-29）
- **問題**:
  - タスク画面に遷移すると、スマホの通知領域と背景が同じ色でかぶる
  - ステータスバーの通知アイコンが見えなくなる
- **修正内容**:
  - ステータスバーのアイコン色を背景色に応じて調整（明るい背景  暗いアイコン）
  - `SystemUiOverlayStyle` で適切な設定を行う
- **実装箇所**: `tasks_screen.dart`
- **追加日**: 2026-01-29

### 45. メール認証待機画面に迷惑メール確認の案内を追加
- **優先度**: 低
- **状態**: ⬜ 未着手
- **内容**:
  - メール認証待機画面で、認証メールが迷惑メールフォルダに振り分けられる可能性がある旨を案内する説明文を追加
  - メールが届かない場合に迷惑メールフォルダも確認するようユーザーを誘導
- **追加日**: 2026-02-16

---

##  名前カスタマイズ（2026-01-29 追加）

### 28. 名前の色フォントカスタマイズ機能
- **優先度**: 中
- **状態**:  未着手
- **内容**:
  - 現在の名前はパーツから選択する形式
  - ユーザーが自由に名前の色やフォントを変更できる機能を追加
  - カスタマイズ可能な項目:
    - 名前の色（カラーピッカーまたはプリセット）
    - フォント（複数フォントから選択）
- **検討事項**:
  - サブスク課金者限定にするか、全ユーザー開放か
  - 不適切な色（背景と同化、見づらい色）への対策
- **実装箇所**: `name_parts_service.dart`, `profile_edit_screen.dart`
- **追加日**: 2026-01-29

---

## 🎭 プロフィール/アバター・徳ポイント（2026-01-29 追加）

### 31. プロフィールアイコンのアバター化（パーツ組み合わせ）
- **優先度**: 中
- **状態**: 🟨 進行中（編集画面は実装済み）
- **更新**: 2026-02-03
  - **編集画面**: `/avatar-edit` を追加（大きいプレビュー/カテゴリ切替/パーツ一覧）
  - **導線**: 設定画面のアバターをタップで編集画面へ遷移（「タップして編集」表示）
  - **カテゴリ表示**: パーツ単体のアセットを表示（組み合わせプレビューは行わない）
  - **ロック機能**: レア度別ロック（rare=徳ポイント / epic=サブスク）
    - 運営アカウントは全解放
    - ロック時はパーツ表示 + 薄いグレー背景 + 右下鍵アイコン
    - レア度枠色: rare=青 / epic=紫
  - **価格設定**: `avatarPartCostsByRarity` を新設（`namePartCostsByRarity` はフォールバック）
  - **背景**: グラデーションに直接配置（白背景/枠線なし）
  - **保存**: 編集画面は「完了」で戻り、設定画面の保存ボタンで反映
  - **アセット/仕様**:
    - 画像サイズ: 1024x1024 の透過PNG
    - 表示枠: 角丸四角
    - 髪と輪郭は同一アセット（hair に統合）
    - パーツ種類: 髪 / 眉 / 目 / 口
    - サイズ/位置: 現在の調整値で固定
    - 選択/保存: 編集画面で選択 → 設定画面で保存
- **内容**:
  - アセットを組み合わせてユーザー独自のキャラクターを作成できる機能
  - パーツ構成は「髪 / 眉 / 目 / 口」で固定
  - レア度は色で表現（ラベル表示なし）
    - Common=一般 / Rare=徳ポイント / Epic=サブスク
    - 表示方法は画面に合わせて調整（スタンプはグロー、他は枠色など）
  - 表示場所: アバター編集画面（実装済み）
- **管理画面のアバター反映**: 保留
  - 理由: 運営のみの画面のため、追加読み取りコストを避ける
- **追加日**: 2026-01-29

### 32. 徳ポイントショップ仕様（購入導線の整理）
- **優先度**: 中
- **状態**: 🟨 進行中（購入処理は完了）
- **内容**:
  - 専用ショップ画面は作らず、各該当画面で購入導線を統一する方針
  - 対象（枝分かれ）:
    - **32-1. 名前パーツ（既存アセット）**
      - 対象: パーツ本体 / 名前パーツのカラー・フォント
      - 購入ダイアログ/購入処理: ✅完了（徳ポイント消費でアンロック）
      - 既存 nameParts の rarity を common/rare/epic に移行（`functions/scripts/migrate-name-parts-rarity.js`）
      - 表示名の同期改善: namePrefix/nameSuffix から displayName を再計算して publicUsers に反映
        - ID揺れ（`prefix_01` / `prefix_pre_01` / 末尾スペース）を正規化
        - 自動フォールバック後にプレビューが `???` になる問題を修正
    - **32-2. リアクションスタンプ（既存アセット）**
      - **状態: ✅完了**
      - ロックUI/ダイアログ/レア度グロー: 実装済み
      - 購入処理: ✅完了（徳ポイント消費でアンロック）
      - アセット追加運用: `functions/scripts/register-reaction-stamps.js` で差分更新（既存は維持、変更分のみ更新）
      - アセット削除運用: `assets/reactions` から削除された自動管理スタンプは、アプリ定義と `settings/virtueShop.reactionCostsById` からも削除
      - 反映順序: Firestore更新を先行し、成功後に `app_constants.dart` を更新（片側のみ更新される事故を防止）
    - **32-3. アバターパーツ**
      - 状態: ✅完了
      - アセット追加運用: `functions/scripts/register-avatar-parts.js` で差分更新（`assets/avatars/*` を再帰走査）
      - アセット削除運用: 対象ファイルが消えたIDは `avatar_assets.dart` / `virtue_shop.ts` から自動除外
      - 命名規則: `<id>_<rarity>.png`（common/rare/epic）
      - 価格運用: アバターはレア度別価格統一のため、価格付き命名（例: `_rare_100`）はエラー
      - Firestore整合: `settings/virtueShop.avatarPartCostsByRarity` をスクリプトで検証/更新可能（`--no-firestore` でスキップ）
  - 将来的な販売候補:
    - アバター枠（アセット）
    - 連続タスク完了の自動投稿装飾アセット
  - UI案:
    - 各画面でロック状態のアイテムを表示
    - ロック状態は鍵アイコン等で明示
    - レア度は色で表現（スタンプはグロー）
  - タップ時の挙動
      - 徳ポイント: 「このアイテムを〇〇徳ポイントで購入しますか？」ダイアログ
      - サブスク: サブスク限定案内 + 課金ページ誘導ボタン（未実装、ボタン文言は「詳細」）
  - 価格設定
    - `settings/virtueShop` に集約（アプリ側のハードコードはしない）
    - `namePartCostsByRarity`（common / rare / epic）
    - `reactionCostsById`（例: shine / star）
  - 解放ルール
    - 一般アイテム: 常時利用可
    - 徳ポイントアイテム: 購入済みのみ利用可（課金の有無は無関係）
    - サブスクアイテム: サブスク加入時のみ利用可（解除で再ロック）
    - **サブスク解除時の自動フォールバック**
      - サブスク限定アイテムを設定中に解約した場合は、一般アイテムへ自動置換
      - 対象: 名前パーツ（前半/後半）、アバターパーツ（髪/眉/目/口）
      - 置換はサーバー側で実施（システムがユーザー設定を書き換える権限が必要）
      - 解約時に案内を表示し、プロフィールなどで設定確認を促す
      - 一般アイテムの選択は「固定 / ランダム」どちらかを決める
  - レア度は色で表現（ラベル表示なし）
    - **全アイテム共通**: Common=一般 / Rare=徳ポイント / Epic=サブスク（3段階固定）
    - スタンプは暫定配分: Common 3 / Rare 2 / Epic 2
  - 表示場所
    - 名前パーツ画面 / 投稿長押しのスタンプ一覧 / アバター編集画面
  - アイテム増加時は各画面の一覧に追加（専用フィルタは当面なし）
  - 導線/文言/購入フローは要議論（実装前に最終確定）
- **追加日**: 2026-01-29

### 32-4. 徳ポイント購入ダイアログUI/運用整理
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-07）
- **背景**:
  - 方針変更を複数回実施し、表示値の参照元や更新タイミングが分かりづらくなっていた
- **現行仕様（確定）**:
  - ダイアログの表示文言は `AppMessages` 経由で管理
  - 表示する保有徳ポイントは `currentUserProvider.virtue` を優先して使用
  - 購入成功時は `currentUserProvider` / `virtueStatusProvider` / `virtueHistoryProvider` を `invalidate` して再取得
  - 購入失敗時は不足/一般エラーをトーストで案内
- **運用決定理由**:
  - Callable直接取得のみだと表示遅延・整合タイミングずれが出やすかった
  - 実際の購入可否はサーバー側で最終判定し、UIは高速表示を優先する
- **関連実装**: `lib/features/home/presentation/widgets/post_card.dart`, `lib/core/constants/app_messages.dart`

### 32-5. アバターパーツ購入ダイアログ統一と初回読み込み安定化
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-07）
- **背景/問題**:
  - アバターパーツ（rare / epic）のダイアログデザインがリアクション側と不統一だった
  - アプリ起動直後に rare パーツをタップすると、価格情報の取得エラーが発生することがあった
- **対応内容**:
  - アバター編集画面の rare / epic ダイアログを、リアクション側と同系デザインへ統一
  - 価格取得を `virtueShopConfigProvider` のキャッシュ -> `future` 読み込み -> `refresh` 再取得 の順でフォールバック
  - 連打時の二重ダイアログ表示を防ぐガードを追加
- **関連実装**: `lib/features/profile/presentation/screens/avatar_edit_screen.dart`

### 32-6. 名前パーツ購入ダイアログ新UI統一
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-07）
- **背景/問題**:
  - 名前パーツ購入ダイアログのみ旧 `AlertDialog` ベースで、アバター/スタンプとUIが不統一だった
  - ロックアイテム連打時に二重ダイアログが開く余地があった
- **対応内容**:
  - rare / epic の購入ダイアログを新UIに統一
  - プレビューは「文言表示 + グロー背景」に変更
  - 価格取得を `virtueShopConfigProvider` のキャッシュ -> `future` -> `refresh` フォールバックに変更
  - 連打時の二重ダイアログ防止ガードを追加
- **関連実装**: `lib/features/profile/presentation/screens/name_edit_screen.dart`

### 29. BANチャット画面の入力欄がナビゲーションバーとかぶる
- **優先度**: 高
- **状態**: ✅ 完了（2026-01-29）
- **問題**:
  - 被BANユーザーと運営のチャット画面で、チャット入力欄がAndroidのナビゲーションバー（戻るホーム履歴ボタン）と重なっている
  - ナビゲーションバーを表示している端末で発生
- **修正内容**:
  - `SafeArea` または `MediaQuery.of(context).viewPadding.bottom` で下部余白を確保
  - ナビゲーションバーの高さ分のパディングを追加
- **実装箇所**: `ban_appeal_chat_screen.dart`
- **追加日**: 2026-01-29

### 46. 新規アカウント登録時にアバターが反映されない
- **優先度**: 中
- **状態**: ⬜ 未着手
- **問題**:
  - 新規アカウント登録フローでアバターを選択・設定した場合、登録完了後にアバターが反映されていない
  - ユーザーが選んだアバターパーツが保存されていないか、登録処理の中で正しく書き込まれていない可能性がある
- **期待値**:
  - 新規登録時に選択したアバターが、登録完了後すぐにプロフィールに反映される
- **追加日**: 2026-02-17

---

##  🌟 徳ポイント（2026-01-29 追加）

### 30. 徳ポイントの仕様見直し（獲得/消失トリガー再設計）
- **優先度**: 高
- **状態**: ✅ クローズ（2026-02-12）
- **背景/問題**:
  - AIモデレーションの誤判定で通常投稿でも違反扱いになり、徳ポイントが減るとUXが悪化する
  - タスク完了での付与は不正に増やしやすい（大量作成→即完了）
  - 他にも獲得/消失のトリガーに抜け/偏りがある可能性
- **対応内容**:
  - 徳ポイントの**獲得トリガー**（例: 有益な行動・継続性・第三者評価など）を再定義
  - 徳ポイントの**消失トリガー**（例: 明確な違反・悪質行為）を再定義
  - 徳ポイント起因のBAN判定を廃止し、BANは管理者判断に統一
  - `decreaseVirtue` の `isBanned` 更新、および `banThreshold` など徳ポイント連動BANロジックを削除
  - 徳ポイント獲得時の即時トーストは表示しない（行動目的のポイント化を避けるため）
  - 徳ポイントはプロフィール/ショップ/履歴で静かに可視化する方針
  - 注記: タスク/目標機能は現時点で廃止方針のため、本トピックでは対象外とする
- **方針決定（2026-02-08時点）**:
  - 徳ポイント0によるBANは行わない（BANは管理者判断のみ）
  - 徳ポイント獲得トーストは表示しない
  - 徳ポイント減少は原則なし。通報対応で運営が投稿削除した場合のみ減少させる
  - 投稿削除時の減少値は固定 `-10`（将来変更しやすいよう `settings/virtuePolicy.adminDeletePenaltyPoints` で管理）
- **ポイント付与経路とスコア（合意済み）**:
  - 投稿作成: `+3`（日次上限 `+15`）
  - 他ユーザーからリアクションを受ける: `+1`（日次上限 `+10`）
  - 他ユーザー投稿へリアクションする: `+1`（日次上限 `+10`）
  - 返信コメントがいいね！された（コメント作成者）: `+5`（日次上限 `+20`）
  - 返信コメントにいいね！した（投稿主）: `+1`（日次上限 `+10`）
  - 通常タスク完了: `+0`（付与なし）
  - タスクストリーク達成:
    - 3日: `+10`
    - 7日: `+20`
    - 14日: `+30`
    - 30日: `+60`
  - 目標達成（基本ポイント）: `+8`（日次上限 `+16`、=1日2回まで満額）
  - 目標達成（証跡ボーナス）: `+22`（証跡が認定された場合のみ）
    - 証跡あり + AI判定OK: 基本 `+8` + ボーナス `+22`（合計 `+30`）
    - 証跡あり + AI判定NG: 基本 `+8` のみ
    - 証跡なし: 基本 `+8` のみ
- **目標証跡の再審査フロー（合意済み）**:
  - AI判定NG時は、再審査ボタンから証跡提出可能（1目標1回）
  - 再審査はAIを使わず、運営が「問題なし / 問題あり」で判定
  - 問題なし: 証跡OKとしてボーナス付与
  - 問題あり: 任意コメント付きでユーザーへ通知
- **追加日**: 2026-01-29
 - **備考**:
   - テスト用として `createPostWithModeration` で投稿ボーナス +100 を付与（2026-02-06）
   - 徳ポイントの加点/減算は `functions/src/helpers/virtue.ts` のヘルパー関数に集約方針（2026-02-06）
   - 方針決定: 徳ポイント獲得トーストは非表示（2026-02-08）
   - 方針決定: 目標達成の基本ポイントは `+8 / 日次上限16`（2026-02-08）
   - 実装: 管理者による投稿削除時のみ減点する `adminDeletePostWithPenalty` を追加。減点値は `settings/virtuePolicy.adminDeletePenaltyPoints`（未設定時10）（2026-02-08）
   - 実装: 加点値/日次上限を `settings/virtuePolicy` 参照へ統一（投稿/リアクション送信/リアクション受信/目標達成/ストリーク）
   - 実装: 旧加点経路（投稿+100テスト、タスク完了+2、完了取消-2）を削除
   - 実装: モデレーション起因の減点経路を削除（`functions/src/callable/posts.ts`, `functions/src/callable/comments.ts`）
   - 実装: 旧ヘルパー減点/加点（`decreaseVirtue` / `increaseVirtue`）を廃止し、実運用経路を `virtue-policy`（加点）、`adminDeletePostWithPenalty`（減点）、`virtue_shop`（消費）に統一
   - 実装: タスクストリーク付与を `grantVirtue` 経由へ統一（`functions/src/triggers/tasks.ts`）
   - 修正: タスクストリーク付与判定を「ユーザーの日次差分」から「`recurrenceGroupId` の直前タスク完了状態」へ変更（過去日付/週次/月次の整合性を改善）
   - 修正: タスク達成の自動投稿（`createTaskCompletionPost`）では投稿作成ボーナスを付与しない（`grantVirtue: false`）
   - 修正: 徳ポイント関連エラーメッセージの英語化を日本語へ戻す（`functions/src/callable/posts.ts`, `functions/src/callable/comments.ts`）
   - クローズ: タスク/目標の徳ポイント設計論点は別トピック「40. タスク/目標機能の安全なクローズ作業」へ移管（2026-02-12）
   - 更新: コメントいいね由来の徳ポイント2経路を追加（受信 `+5/日次20`, 送信 `+1/日次10`）し、投稿/リアクション受信の日次上限を調整（投稿 `15`, 受信 `10`）（2026-02-13）

### 30-1. テスト投稿ボーナス（+100）の本番前撤去チェック
- **優先度**: 高
- **状態**: ✅ 完了
- **背景/問題**:
  - 以前は検証用に投稿時の徳ポイント加点を大きく設定していた
  - このまま本番投入すると徳ポイント経済・購入導線のバランスが崩れる
- **対応内容**:
  - `createPostWithModeration` / `createPostWithRateLimit` の固定テスト加点を廃止
  - 投稿加点を `settings/virtuePolicy.postCreatePoints` / `postCreateDailyCap` に移行
  - 変更後に徳ポイント購入導線（不足/成功）を再テスト
  - リリースチェック項目に「テスト加点無効化確認」を追加
- **実装箇所**: `functions/src/callable/posts.ts`, `functions/src/helpers/virtue.ts`
- **追加日**: 2026-02-07

---

##  コメント機能（2026-02-02 追加）

### 33. コメントへの投稿主限定リアクション（お礼スタンプ機能）
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-12）
- **概要**:
  - 投稿主が返信コメントへお礼を返し、返信コメント投稿者が「スタンプ回数」を獲得する
  - ボトムナビのタスク導線をスタンプシート導線へ段階移行する
- **Phase 1 仕様（確定）**:
  - 投稿主限定で返信コメントへお礼いいね可能（返信コメント長押し -> 候補表示 -> タップ確定）
  - 同一コメントへのいいねは1回のみ
  - いいね取り消しは不可
  - いいねされた返信コメント投稿者にスタンプ回数 `+1`
  - いいね付与時の加算値は `+1`（`functions/src/callable/comments.ts` の `THANKS_STAMP_CREDITS_PER_LIKE`）
  - スタンプ回数は総数のみ管理（種類別管理なし）
  - ボトムナビのタスクタブをスタンプシートタブへ置換（タスク機能は非表示のみ）
  - スタンプシートの空き枠タップでスタンプバー表示、選択スタンプ配置時に回数 `-1`
  - 配置済み枠の上書きは回数消費なし
  - シートで選択できるスタンプは既存リアクションスタンプの解放状態と同期
  - スタンプシート台紙はアセット運用し、台紙にも `Common / Rare / Epic` を適用
  - 台紙デザインと押下枠は `sheetId + layout JSON` で同期（正規化座標 + `slotId` 不変運用）
- **ドキュメント**:
  - `docs/COMMENT_THANKS_STAMP_SHEET_SPEC.md`
- **実装済み箇所**:
  - `lib/features/home/presentation/widgets/comment_card.dart`
  - `lib/features/home/presentation/screens/main_shell.dart`
  - `lib/features/stamp_sheet/`（新規）
  - `functions/src/callable/comments.ts`（または専用 callable 追加）
- **追加日**: 2026-02-02

---

## 🔐 ログイン画面（2026-02-04 追加）

### 34. ログイン画面のUI修正
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-04）
- **問題**:
  1. ログイン画面の下部に不要な白いスペースがある
  2. 「アカウントをお持ちでない？」という文言が分かりにくい
- **修正内容**:
  - 下部の白スペースを削除（全画面グラデーションに変更）
  - 文言を短く整理
    - ログイン: 「はじめてですか？」＋「新規登録」
    - 新規登録: 「登録済みですか？」＋「ログイン」
  - 新規登録画面で **アイコン / アバター（パーツ）** を切替できるように変更
    - 新規登録時は Common パーツのみ表示
  - 旧アイコンの枠を **角丸四角** に統一
- **参考スクリーンショット**: `docs/error/Screenshot_20260204_132257.jpg`
- **実装箇所**: `login_screen.dart`
- **追加日**: 2026-02-04

### 35. メール認証の必須化
- **優先度**: 高
- **状態**: ✅ 実装済み（確認完了 2026-02-05）
- **問題**:
  - Firebase Auth の仕様上、存在しないメールアドレスでも登録できてしまう
  - メールの実在性確認が行われていないため、不正/誤登録のリスクがある
- **対応内容**:
  - 新規登録時に `sendEmailVerification()` を実行
  - 認証完了まで本体利用をブロック（認証待機画面へ誘導）
  - 認証待機画面は **メール変更を不可** に変更
    - 入力欄は廃止し、登録時のメールアドレスを文言で表示
    - 再送は **同一メールのみ**
  - 再送ボタン（60秒クールダウン中は無効・薄色）
  - 「認証できたらここをタップ」→ 成功時は **/home に遷移**
  - 「登録をやり直す」→ **/register へ遷移**
  - 未認証ユーザーの自動削除（Auth + Firestore）
    - **7日経過で削除**（`cleanupUnverifiedUsers` を **毎日 4:00** に実行）
  - 既存ユーザー向けに emailVerified を一括更新するスクリプトを用意
    - `node functions/scripts/backfill-email-verified.js`
  - 認証状態の監視は `authStateChanges` → `userChanges` に変更
  - ルーターは `refreshListenable` 方式で再評価のみ行い、サインアウト時の遷移不安定を改善
  - 認証/登録画面の文言は AppMessages に集約（ハードコード廃止）
  - 登録画面の名前パーツは **Common のみ** を `NamePartsCatalog` から参照
- **確認内容**:
  - 認証待機画面から「登録をやり直す」→ **/register へ遷移**
  - 認証完了 → **/home へ遷移**
  - 未認証ユーザーが自動削除されることを確認（現在は 7日/1日1回 に戻し済み）
- **追加日**: 2026-02-04

### 36. 認証待機画面のUI調整
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-05）
- **問題**:
  - 認証待機画面が見にくく、UXが悪い
- **対応内容**:
  - メールアイコン＋パルスアニメーション（視認性強化）
  - タイトルを大きく表示（認証待機が一目でわかる）
  - メールアドレスをピル型カードで独立表示
  - 手順カード（ステップ表示）でやることを整理
  - 主要ボタン（認証確認）を強調
  - 再送/登録やり直しボタンを横並びで整理
  - 文言を `AppMessages` に集約
- **追加日**: 2026-02-05

### 37. PayWall画面のUI改善
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-05）
- **問題**:
  - PayWall画面が視覚的に弱く、購入意欲を刺激しにくい
  - 購入処理中のオーバーレイがグレー背景+白テキストで味気ない
- **対応内容**:
  - ヒーローアイコン（王冠）+ グローアニメーション（パルス効果）
  - 価格表示を `displaySmall` で大きく強調（￥500/月 形式）
  - 機能リストカードをガラスモーフィズム風に変更
  - 購入中オーバーレイをブラー背景 + 白カード + プライマリ色スピナーに改善
  - 「少々お待ちください」メッセージを追加
  - 文言を `AppMessages` に集約
- **追加日**: 2026-02-05

### 38. プロフィール編集でアイコン/アバター選択（旧アイコン対応）
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-13）
- **内容**:
  - プロフィール編集で **[アイコン] [アバター] [画像設定]** の切替ボタンを設置（新規登録画面と同系UI、トグルではなくボタン切替）
    - **アイコン**: 旧アイコン一覧を表示
    - **アバター**: アバター編集画面へ遷移
    - **画像設定**: カスタム画像設定導線（サブスク限定）
  - **画像設定** は非サブスク時に鍵アイコンでロック表示（押下時は既存プレミアム導線に接続）
  - アイコン/アバター/画像設定の切替時は前回選択状態を保持し、保存ボタン押下で反映（即時保存しない）
  - 旧アイコンの枠は **角丸四角** で統一
  - 画像設定の仕様はサークル画像設定に準拠
    - 画像選択 -> クロップ -> 保存
    - クロップ比率・画質運用は既存のサークル画像設定に合わせる
    - モデレーションは **簡易チェック + AIモデレーション**（運営目視レビューなし、サークル画像設定と同一方針）
  - 表示適用:
    - TL/コメント/プロフィール等のアイコン枠には、`profileVisualMode` に応じて icon/avatar/image を即時反映
  - サブスク失効時:
    - `profileVisualMode` を自動で `icon` に戻す
    - 復帰先アイコンは「最後に選択していたアイコン」（未設定時はデフォルト）
  - 画像ロック導線:
    - 非サブスクで `画像設定` 押下時は即購入ダイアログ表示（既存Epic購入系UI）
    - 文言: `サブスク限定` / `この機能はサブスク課金限定の機能だよ` / ボタン `画像アイコン機能解放`
  - データ方針:
    - 保持フィールドは最小構成（`profileVisualMode`, `profileImageUrl`, `profileImageStoragePath`）
    - `profileImageModerationStatus` は保持しない
  - 画像削除方針:
    - 専用の削除導線は作らない
    - 画像差し替え時・icon/avatar切替時・サブスク失効時に旧画像をStorageから削除
  - 既存ユーザー初期値:
    - `profileVisualMode = icon`
  - 実装メモ（2026-02-13）:
    - 設定画面に `アイコン / アバター / 画像設定` 切替を実装
    - 画像設定は `ImagePicker + Crop(1:1) + 簡易チェック + AIモデレーション` を通して保存
    - 非サブスクで `画像設定` 押下時は指定文言の即購入ダイアログを表示
    - `profileVisualMode/profileImageUrl/profileImageStoragePath` を users/publicUsers に反映
    - TL/コメント/プロフィール等のアバター表示で `profileVisualMode` を適用
    - サブスク失効時は Functions 側で `image -> icon` 自動復帰 + 旧画像削除
    - 画像差し替え/モード変更時の旧画像削除が効くよう `firebase/storage.rules` の profile delete 条件を修正
  - 動作確認:
    - 実機でプロフィール画像の変更・モード切替・旧画像削除を確認済み
- **追加日**: 2026-02-05
- **更新日**: 2026-02-13

### 39. 文字化け再発防止ルールの運用
- **優先度**: 中
- **状態**: ✅ 完了（2026-02-08）
- **背景**:
  - PowerShell経由のファイル書き込みで文字コード崩れが発生しやすい
  - Cloud Functions/Flutter で日本語コメント・文言が文字化けした実績がある
- **対応内容**:
  - 運用ルール文書を追加: `docs/ENCODING_AND_MESSAGE_RULES.md`
  - 文字化け検知スクリプトを追加: `scripts/check-mojibake.js`
  - 実行コマンドを追加: `npm run check:mojibake`
  - Gitフックを追加: `.githooks/pre-commit`（コミット時に自動チェック）
  - Flutter文言は `AppMessages`、Functions文言は `functions/src/config/messages.ts` に集約する境界を明文化

### 40. タスク/目標機能の安全なクローズ作業
- **優先度**: 高
- **状態**: ✅ 完了（2026-02-12）
- **背景**:
  - タスク/目標機能を廃止方針にしたため、単純な非表示だけでなく安全な段階的クローズが必要
  - 中途半端に導線やジョブが残ると、通知・加点・データ整合で回帰リスクが出る
- **対応内容**:
  - クローズ範囲を明確化（UI導線停止、Callable停止、Trigger停止、定期ジョブ停止、関連通知停止）
  - 既存データの扱いを決定（保持 / アーカイブ / 削除）し、復旧不能操作の前にバックアップ方針を定義
  - 徳ポイント加点経路でタスク/目標由来が残っていないことを最終監査
  - Firestore Rules / Router / Feature Flag / 監視項目を含むリリース手順書を作成
  - ロールバック手順（再有効化時の順序・影響範囲）を定義
- **完了条件**:
  - 本番でタスク/目標由来の通知・加点・画面遷移が発生しない
  - 既存ユーザーデータに破壊的影響がない
  - 運用ドキュメント（クローズ手順・監視・ロールバック）が `docs/` に存在する
- **実施済み（2026-02-12）**:
  - Flutter側のタスク/目標画面・モデル・サービス・プロバイダを削除
  - ルーターから `/tasks` `/goals` `/monthly-calendar` 導線を削除
  - 通知遷移の task/goal ルートを `/stamps` フォールバックへ変更
  - Cloud Functions の task/goal callable・trigger・reminder エントリを削除
  - Firestore / Storage Rules と indexes から task/goal 関連定義を削除
  - Firestore 既存データのクリーンアップ（tasks / goals / autoPostSettings / task_attachments / task-goal 通知）
  - `AppMessages` の task/goal 由来未使用文言を削除
  - 関連設計資料へ「廃止済み・履歴資料」注記を追加し、実装との不一致を解消
- `flutter analyze` / `functions build` / `check:mojibake` による最終監査を実施

### 41. 初回起動ユーザー向けチュートリアル（オリジナルキャラクター案内）
- **優先度**: 中
- **状態**: 🟡 Phase 4 完了（2026-02-17 更新）
- **背景**:
  - 初回ユーザーが主要導線（投稿/リアクション/スタンプ/プロフィール）を把握しづらい
  - アプリオリジナルキャラクターの吹き出し案内で、離脱前に基本操作を理解してもらう
- **確定仕様**:
  - 対象ユーザー:
    - 初回起動ユーザーのみ
    - 途中離脱後の再案内は行わない
  - 表示タイミング:
    - 初回ホーム画面表示時に開始
  - UI:
    - 画面全体を薄いグレーでラップ
    - キャラクター + 吹き出しで案内
    - 重要操作では対象以外のタップを抑制し、ユーザーに実操作させる
  - 操作:
    - 単発ステップ: `完了` のみ
    - 複数ステップ: `次へ` + `スキップ`
  - 永続化:
    - `users/{uid}` に表示済み状態を保存（端末ローカル保存は不採用）
  - 再表示導線:
    - チュートリアル再表示機能は実装しない
    - ヘルプ配下にテキスト資料を用意して補完（資料は別タスク）
  - 実装方針:
    - 機能ごとに段階実装（Phase方式）
- **実装優先順（Phase）**:
  - Phase 1（最優先）:
    - 設定画面の公開範囲（AI/MIX/HUMAN）説明
    - このステップでユーザーに公開範囲を選択させる
    - ホーム画面（機能概要 + 投稿カード長押しリアクション）
  - Phase 2:
    - 投稿詳細画面（機能概要 + 投稿主の返信コメント長押しいいね）
  - Phase 3:
    - スタンプ画面（長押し押印 / シート一覧 / コレクション / 取り消し）
  - Phase 4:
    - ボトムナビ説明（ホーム / サークル / 投稿 / スタンプ / マイページ）
  - Phase 5:
    - サークル画面（機能概要 + FAB挙動説明）
    - 非サブスクユーザーは初回のみ閲覧許可（閲覧専用・詳細仕様は #44 で管理）
    - 閲覧専用の定義:
      - サークル一覧/詳細の閲覧は可能
      - サークル作成は不可
      - 参加・参加申請は不可
    - 画面遷移またはアプリ終了で再ロック
  - セキュリティ要件（サークル制限）:
    - UIロックだけに依存せず、サーバー側で非サブスクの作成/参加/参加申請を拒否する
    - 対象:
      - `createCircle`（作成）
      - `joinCircle`（公開サークル参加）
      - `sendJoinRequest`（招待制参加申請）
    - Firestore Rules でも「非サブスク作成不可」を担保し、改造クライアント経路を閉じる
  - Phase 6:
    - マイページ（機能概要 + 徳ポイント概念 + 履歴導線 + お気に入り）
- **完了条件**:
  - 初回ユーザーでのみ自動表示される
  - スキップ/完走のどちらでも通常利用へ遷移できる
  - 公開範囲説明（Phase 1）が確実に完了し、誤モード利用を抑制できる
  - 非サブスク初回サークル閲覧の一時許可が想定どおりに失効する
- **実施済み（2026-02-15 / Phase 1 着手）**:
  - `users/{uid}` に `tutorialPhase1Completed` フラグを追加（既存ユーザー移行は未対応。未公開運用のため影響なし）
  - 新規登録ユーザーは `tutorialPhase1Completed=false` で作成
  - 初回ホーム表示時に、公開範囲（AI/MIX/HUMAN）を必須選択させるチュートリアルダイアログを実装
  - キャラクター画像は `assets/onbord/onbord_01.png` を利用
  - 選択確定時は `postMode` のみ更新し、`tutorialPhase1Completed` は未更新のまま進行
- **実施済み（2026-02-16 / Phase 1 安定化）**:
  - `settingsScroll` で公開範囲カードへ自動スクロール後、閉じたカードを先にピックアップする進行へ統一
  - 説明ステップ（AI/MIX/HUMAN）は画面タップ進行、`finished` で最終モード選択へ遷移
  - `finished` で1回タップするとオーバーレイを閉じ、通常操作でモード選択できる仕様に調整
  - ピックアップ描画は「マスク穴の脈動のみ」に方針確定（赤枠線/グローは不採用）
  - チュートリアル本文の太さを `fontWeight: FontWeight.w400` で固定し、画面差による太さブレを解消
  - 注意: `Scrollable.ensureVisible` を使うステップでは、対象ウィジェットが初回ビルド時に生成済みであることを前提に構成する（未ビルドだと `currentContext == null` で詰む）
- **実施済み（2026-02-16 / ホーム説明ステップ）**:
  - `finished` 後の進行として `homeOverview` → `homeLongPress` を追加
  - ホーム概要は全体マスクのみで説明し、投稿カード長押し説明は投稿カードをピックアップ
  - `homeLongPress` は「画面タップ完了」を廃止し、実際の投稿カード長押し操作でのみ完了する仕様に変更
  - `homeLongPress` 完了時に `tutorialPhase1Completed=true` / `tutorialPhase1Step=null` を更新
  - スポットライト穴はタップ透過（周辺マスクのみタップブロック）として、実操作誘導を担保
- **実施済み（2026-02-16 / 長押しステップ離脱対策）**:
  - `homeLongPress` 中はタイムライン縦スクロールを無効化（投稿カード内スクロールでの離脱を防止）
  - `homeLongPress` 中は `RefreshIndicator` を無効化（下スワイプ更新で通常状態へ戻る経路を遮断）
  - `homeOverview` / `homeLongPress` 中は `TabBarView` 横スワイプを無効化（フォロー中タブへの遷移を防止）
  - `homeLongPress` 対象カードはタップ遷移（投稿詳細・内部タップ操作）を無効化し、長押し操作のみ受け付ける制御に変更
  - チュートリアル用プローブはタップ吸収を廃止し、カード本体の長押しハンドラ（リアクションスタンプバー表示）を通す構成へ変更
  - 長押し判定に移動量キャンセル（閾値超過で失効）を追加し、ドラッグ由来の誤完了を防止
- **実施済み（2026-02-16 / Phase 2 投稿詳細チュートリアル）**:
  - 対象は「自分の投稿の投稿詳細画面」に初めて遷移したときのみ開始
  - `users/{uid}` に `tutorialPhase2Completed` / `tutorialPhase2Step` を追加し、進行状態を永続化
  - Step 1: 投稿詳細画面の機能概要を全画面グレーラップで案内（AppBar領域まで含めて表示）
  - Step 2: 他ユーザーコメントカードをピックアップし、長押しで「いいね」できる導線を案内
  - ピックアップ対象はコメントカード本体のみ（アバターは除外）
  - Step 2中はスクロール・コメント入力など長押し以外の操作を抑制
  - コメントカード長押し時点で Phase 2 を完了扱いにし、同時にいいねボトムシート表示は継続
  - マスク穴は pass-through 時も脈動し、2重マスクに見えない描画へ調整済み
- **実施済み（2026-02-16 / Phase 3 スタンプチュートリアル）**:
  - `users/{uid}` に `tutorialPhase3Completed` / `tutorialPhase3Step` を追加し、進行状態を永続化
  - 起点はスタンプ画面初回表示に設定し、概要 → シート長押し → シート一覧 → コレクション → 取り消しの順で案内
  - Phase 3 では AppBar まで覆うグレーラップ + 脈動マスク穴を採用し、座標再計算を統一して復帰時の縦ズレを解消
  - シート一覧/コレクションの説明は実際に遷移させ、戻った時点で次ステップへ進む導線に変更
  - 取り消しステップは実際の Undo 操作で完了する仕様に変更
  - 初回シート未選択時はシート本体を表示せず、画面内の「デザイン選択」ボタンをチュートリアル起点に変更
  - 初回シート選択後の再判定 race を解消し、二重で「最初のシート選択」を要求しないよう修正
  - Phase 3 中はボトムナビ操作を無効化し、チュートリアル離脱経路を遮断
  - 新規ユーザーの初期 `thanksStampCredits` は 1 ポイントで作成するよう変更（Auth 作成時）
  - 注記: `users/{uid}` へのクレジット直接更新は権限拒否が発生するため、チュートリアル側の一時加算ロジックは採用しない方針に統一
- **実施済み（2026-02-17 / Phase 4 ボトムナビ軽量案内）**:
  - 開始タイミングを `homeLongPress` 成立時ではなく、リアクションスタンプバーを閉じた後に変更
  - 実装は `PostCard` の `onReactionOverlayClosed` コールバックで行い、閉じる操作完了後に `bottomNavHome` へ遷移する
  - ボトムナビ説明は独立フェーズではなく「Phase 1の延長」として実装
  - 5ステップで順番にピックアップ（ホーム → サークル → 投稿ボタン → スタンプ → マイページ）
  - 説明文言は `AppMessages.tutorial.bottomNavHomeGuide` / `bottomNavCircleGuide` / `bottomNavStampGuide` / `bottomNavMyPageGuide` に追加
  - サークル説明では「サブスク限定」「初回1回のみ閲覧可」「画面遷移/アプリ終了で終了」を明示
  - ボトムナビ案内ステップ中はホーム外へ遷移しないよう、`main_shell` の強制遷移条件に含めた

### 42. 投稿/コメントのレートリミット運用（コスト防衛）
- **優先度**: 高
- **状態**: ✅ 実装済み（2026-02-13）
- **背景**:
  - 投稿/コメントの連投により AI 実行コストが増加するため、サーバー側で明確な上限管理が必要
  - クライアント広告表示だけでは防衛しきれない（Functions 直接呼び出し・改造クライアント対策）
- **現行仕様（確定）**:
  - 投稿:
    - 60秒あたり2回まで（3回目で拒否）
    - 1日15回まで（JST日付単位）
    - 対象関数: `createPostWithModeration`
    - エラー文言:
      - 60秒制限: `短時間での投稿が多すぎます。少し待ってから再度投稿してください`
      - 日次制限: `1日に投稿できる回数は15回までです。明日また投稿してください`
  - コメント:
    - 60秒あたり2回まで（3回目で拒否）
    - 対象関数: `createCommentWithModeration`
    - エラー文言: `コメントは60秒で2回までです。少し待ってから再度送信してください`
  - 投稿広告:
    - 3投稿ごと表示は維持
    - 日次表示上限は撤廃
- **実装メモ**:
  - 投稿/コメントともに `users/{uid}` のレートカウンター方式で判定（クエリ依存を減らして安定化）
  - 投稿はデバッグ用に `[POST_RATE]` ログを出力し、日次/短期判定値を追跡可能
  - 60秒制限エラーはネガティブ判定ダイアログではなく通常エラー表示（Snackbar）へ分岐
- **関連ファイル**:
  - `functions/src/callable/posts.ts`
  - `functions/src/callable/comments.ts`
  - `functions/src/config/messages.ts`
  - `lib/shared/services/post_ad_policy_service.dart`
  - `lib/features/post/presentation/screens/create_post_screen.dart`
  - `lib/features/post/presentation/screens/post_detail_screen.dart`
- **デプロイ手順（個別）**:
  - 投稿制限: `firebase deploy --only functions:createPostWithModeration`
  - コメント制限: `firebase deploy --only functions:createCommentWithModeration`
  - まとめて反映: `firebase deploy --only functions:createPostWithModeration,functions:createCommentWithModeration`

### 44. 非サブスク向けサークル初回閲覧（1回限定・閲覧専用）
- **優先度**: 中
- **状態**: ✅ 実装済み（2026-02-15）
- **背景**:
  - チュートリアル導入前に、サークル機能の雰囲気だけを初回ユーザーへ安全に体験させたい
  - 非サブスクユーザーの作成/参加導線は厳密に遮断しつつ、閲覧のみ許可する必要がある
- **確定仕様**:
  - 対象はアカウント単位で1回のみ
  - 非サブスクユーザーは初回のみサークル画面を閲覧可能
  - 体験中でも作成/参加/参加申請は不可
  - 失効条件:
    - サークル画面から別画面へ遷移した時
    - アプリを完全終了した時
  - バックグラウンド移行では失効しない
  - 判定はサーバー側で保持（`users/{uid}` に初回利用済みフラグ）
  - サークル画面に「体験中」の表示を出す
- **セキュリティ要件**:
  - Cloud Functions 側で非サブスクの `joinCircle` / `sendJoinRequest` を拒否
  - Firestore Rules 側で非サブスクの `circles` 作成を拒否
  - `users/{uid}` の試用管理フィールドはクライアント更新不可
- **実装メモ**:
  - 初回タップ時に `startCircleBrowseTrial` を実行し、サーバー側の1回制限を判定
  - 閲覧セッション中はサークル画面に体験中バナーを表示
  - 体験中説明文に「画面遷移/アプリ終了で終了し、再閲覧不可」を明記

---

## 33-実装進捗（2026-02-12）

### 実装済み
- タスクタブを `スタンプ` タブへ置換（`/stamps`）。
- コメント長押し導線から投稿主限定 `いいね！` 付与を実装（取り消し不可・1コメント1回）。
- `いいね！` 付与時の加算先を修正（投稿主ではなくコメント投稿者へ `thanksStampCredits` を加算）。
- 加算値を仕様どおり `+1` に確定（`THANKS_STAMP_CREDITS_PER_LIKE = 1`）。
- コメントカード文言を調整（付与済み表示は「いいね！されたよ」）。
- スタンプシートを「シート長押し -> スタンプ選択 -> 空き枠へ左上から順押し」に変更。
- 押印時は `thanksStampCredits -1` を消費（空き枠がない場合は追加不可）。
- ロック状態をリアクション解放状態と同期（free / rare / epic）。
- いいね実行時を楽観UI化（タップ確定直後に表示反映、失敗時のみロールバック）。
- いいね実行側トースト文言を `いいね！しました` に統一。
- スタンプ押印をハイブリッド同期へ変更。
  - 押印時はUI即時反映（楽観）
  - 押印アクションは端末内キューへ永続化（未送信保持）
  - 再送タイミングは「起動時 + スタンプ画面再表示時 + ライフサイクル離脱時」
  - TTL（24時間）と件数上限（200件）でキュー肥大化を抑制
  - 同期失敗通知は1回のみ表示
- 現行方針の引き継ぎ資料を追加（2026-02-11）:
  - `docs/STAMP_SHEET_CURRENT_STRATEGY.md`
- アーカイブ一覧画面を実装（2026-02-11）:
  - 画面: `lib/features/stamps/presentation/screens/stamp_sheet_archives_screen.dart`
  - 導線: `lib/features/stamps/presentation/screens/stamp_sheet_screen.dart` のAppBarアイコン
  - ルート: `/stamps/archives`
- デザイン一覧画面を新規実装（2026-02-12）:
  - 画面: `lib/features/stamps/presentation/screens/stamp_sheet_catalog_screen.dart`
  - 導線: `lib/features/stamps/presentation/screens/stamp_sheet_screen.dart` のAppBarアイコン
  - ルート: `/stamps/catalog`
  - 表示: 2列グリッド、所持はチェックバッジ、未所持はロックバッジ
  - 購入: rare は既存購入確認文言に沿ったダイアログ経由、epic は `/premium` へ誘導
- Firestore Rules を更新（2026-02-12）:
  - `users/{uid}/stampSheetArchives/{archiveId}` を owner read のみに許可（write 禁止）
- スタンプ画面のUI導線を更新（2026-02-12）:
  - 取り消しはアイコンのみ
  - デザイン一覧をボトムシート導線からAppBarの新規画面導線へ移行

### 34. スタンプ押印時のアニメーション追加
- **優先度**: 中
- **状態**: ⬜ 未着手
- **内容**:
  - スタンプシートにスタンプを押す際のアニメーション演出を追加
  - 押印の満足感・達成感を高めるマイクロインタラクション
- **追加日**: 2026-02-16

### 残作業
- スタンプシート Firestore 設定の運用ガイド（本番運用手順）整備。
